h2><%= @doctor.name %></h2>
h2><%= @doctor.department %></h2>
 
Appointments:
  <% @doctor.appointments.each do |app| %>
    <%= app.appointment_datetime.real_time %>
    <%= link_to app.patient.name, patient_path(app.patient) %> 
  <% end %>

